[tool.poetry]
name = "psrdb"
version = "0.1.0"
description = "CLI for pulsars.org.au"
authors = ["GWDC"]
license = "MIT"
packages = [{include = "psrdb"}]

[tool.poetry.dependencies]
python = "^3.8"
requests = "^2.25.1"

[tool.poetry.dev-dependencies]
pytest = "^7.3.1"

[tool.poetry.scripts]
"psrdb.py" = "psrdb.scripts.psrdb:main"
"ingest_ptuse_folded.py" = "psrdb.scripts.ingest_ptuse_folded:main"
"ingest_ptuse_search.py" = "psrdb.scripts.ingest_ptuse_search:main"
"ingest.py" = "psrdb.scripts.ingest:main"
"remove_fluxcals.py" = "psrdb.scripts.remove_fluxcals:main"


[tool.poetry.group.dev.dependencies]
sphinx = "^7.0.1"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
